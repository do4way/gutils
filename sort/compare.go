package sort

//Min find the minimum from the two integer.
func Min(a, b int) int {
	if a < b {
		return a
	}
	return b
}

//Max find the maximum from the tow integer
func Max(a, b int) int {
	if a > b {
		return a
	}
	return b
}
